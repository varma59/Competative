def getMinMax( a, n):
    # max = a[0]
    # min = a[0]
    # for i in range(1,n):
    #     if(a[i])>max:
    #       max=a[i];
    #     elif(a[i])<min:
    #         min=a[i];
        
    # maxmin = min ,max;
    # return maxmin    
    
    return min(a),max(a)
